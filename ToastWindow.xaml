<Window x:Class="SoundVibe.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SoundVibe Bildirim" Height="100" Width="330"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Opacity="0" Loaded="Window_Loaded">

    <Window.Resources>
        <SolidColorBrush x:Key="ThemeAccentBrush" Color="#FFD13B8A"/>
        <SolidColorBrush x:Key="ThemeGlassBrush" Color="#B0000000"/>
        <SolidColorBrush x:Key="ThemeDarkBgBrush" Color="#60000000"/>
    </Window.Resources>

    <Border Margin="15" Background="{DynamicResource ThemeDarkBgBrush}" CornerRadius="12" BorderBrush="#40FFFFFF" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Color="Black" Opacity="0.8" ShadowDepth="4"/>
        </Border.Effect>
        <Border Background="{DynamicResource ThemeGlassBrush}" CornerRadius="12">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="45"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="45" Height="45" CornerRadius="8" Background="#111" VerticalAlignment="Center">
                    <Border CornerRadius="8" ClipToBounds="True">
                        <Image x:Name="ImgToastCover" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0,0,0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <TextBlock Text="🎵" FontSize="10" Foreground="{DynamicResource ThemeAccentBrush}" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="ŞİMDİ ÇALIYOR" FontSize="9" FontWeight="Bold" Foreground="#90A4AE" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock x:Name="TxtToastTitle" Text="Şarkı Adı" Foreground="White" FontWeight="Bold" FontSize="13" TextTrimming="CharacterEllipsis"/>
                    <TextBlock x:Name="TxtToastArtist" Text="Sanatçı" Foreground="#B0C4DE" FontSize="11" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Grid>
        </Border>
    </Border>
</Window>